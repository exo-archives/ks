<%  
	import org.exoplatform.forum.ForumUtils;
	import org.exoplatform.forum.ForumSessionUtils;
%>
<div class="UIBreadcumbs" id="$uicomponent.id">
	<div class="LeftBreadcumbs">
		<div class="RightBreadcumbs"> 
			<div class="CenterBreadcumbs">
				<div class="BreadcumbsInfoBar">
					<div class="OpenIcon"><span></span></div>
				<% int t = 1 ;
					for(namePath in uicomponent.getBreadcumbs()) {
						String link = uicomponent.url("ChangePath", uicomponent.getPath(t-1)) ;
						String event = uicomponent.event("ChangePath", uicomponent.getPath(t-1)) ;
						if(namePath.equals(ForumUtils.FIELD_EXOFORUM_LABEL)) {
							namePath = _ctx.appRes("UIBreadcumbs.label.eXoForum");
						}
						if(namePath.equals(ForumUtils.FIELD_SEARCHFORUM_LABEL)) {
							namePath = _ctx.appRes("UIBreadcumbs.label.SearchForums");
						}
						if(t < uicomponent.getMaxPath()) {
				%>
					<a href="$link" onclick="$event; return false;" class="Selected">$namePath</a>
					<div class="RightArrowIcon"><span></span></div>
				 <% } else {
				 		 	if(uicomponent.isLink()) {
				 %>
					<a href="$link" class="Selected">$namePath</a>
					<% 	} else{ %>				 
					<div class="Selected">$namePath</div>
				 <%		
				 			}
				 		} 
				  t = t + 1 ;
				  } 
				 %>
			  	<div style="clear: left"><span></span></div>
				</div>
				<div class="RSSBar">
					<%if(t == 2 && !ForumSessionUtils.isAnonim()){
						String newMessage = _ctx.appRes("UIBreadcumbs.label.PrivateMessages") ; 
						if(uicomponent.getNewMessage() > 0) {
						  newMessage = "<b>"+newMessage+"(" + uicomponent.getNewMessage() + ") </b>";
						} else {
							newMessage = newMessage + "&nbsp;" ;
						}
					%>
					<a style="float:left; line-height: 24px;" href="<%=uicomponent.event("PrivateMessage");%>">
						 $newMessage
					</a>
					<a style="float:left" href="<%=uicomponent.event("Rss");%>" >
					<%} else {%>
					<a href="<%=uicomponent.event("Rss");%>" >
					<%}%>
						<div class="SeparatorLine">
							<div class="RSSIcon_" style="height: 25px;" ><%//=_ctx.appRes("UIBreadcumbs.label.Rss");%></div>	
						</div>	
					</a>
					<%if(t == 2 && !ForumSessionUtils.isAnonim()){%>
					<div style="clear: left"><span></span></div>
					<%}%>				
				</div>
				<div style="clear: both"><span></span></div>
			</div>
		</div>
	</div>
</div>